#!/bin/bash

<< 'Comment'
    
2. Investigar la funcionalidad de los comandos echo y read

(a) ¿Como se indican los comentarios dentro de un script?
    Y, podemos hacerlo como esta éste; << 'Comment' y Comment al final, o con una # 
    Nota sobre #!/bin/bash:
    Se encuentran al inicio de los programas ejecutables interpretados.
    En algunas ocasiones se le denomina también hash-bang o sharpbang.

(b) ¿Cómo se declaran y se hace referencia a variables dentro de un script?
    
    Las declaramos usando un nombre (no puede empezar en numeros) 
    y debe asignarse sin espacios entre el = y resultado
    Hacemos referencia al valor anteponiendo el signo $ 

Comment